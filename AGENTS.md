# AGENTS.md — 研究室行政流程知識庫（中研院經濟所通用版）

本資料夾是一份**行政流程知識庫**，不是程式專案：沒有要建置、測試或執行的東西。
使用者（研究室的專任助理或老師）會問請購、報帳、差旅、聘僱、公文等流程問題，
你的工作是**照這份文件回答，並幫忙把單據內容擬好**。

本檔供讀取 `AGENTS.md` 的 AI 工具使用（OpenAI Codex、Cursor、Gemini CLI 等）。
Claude Code / Claude.ai / GitHub Copilot 走 `SKILL.md`，兩者內容同源，
**流程規則只寫在 `procedures/` 裡，改規則就改那裡的檔案，不要改這兩個入口檔。**

## 最重要的一條

回答任何流程問題之前，**先完整讀 `SKILL.md`**。那裡有總覽、跨流程共通規則
（統編、匯率換算、財產 vs 物品、會計關帳時間等）以及「什麼情境 → 讀哪個檔案」的路由表。
讀完路由表指到的子檔案再動手，不要憑檔名猜內容。

多數任務會同時牽涉「請購」與「報帳」兩個階段，需要時就兩個檔案一起讀。
判斷不出屬於哪一類時，先讀 `procedures/reimbursement.md`。

## 檔案地圖

| 檔案 | 內容 |
| --- | --- |
| `SKILL.md` | **入口**：總覽、跨流程共通規則、完整路由表 |
| `procedures/procurement.md` | 請購、採購、設備、財產、電子勞務（訂閱軟體）請購 |
| `procedures/reimbursement.md` | 報帳、核銷、發票收據、黏存單、借據、匯率換算 |
| `procedures/acceptance-scrapping.md` | 驗收、財產貼紙、報廢、汰舊換新、財產移動 |
| `procedures/travel.md` | 國外差旅、出國申請、機票、公假、日支數額 |
| `procedures/part-time-assistant.md` | 兼任助理、勞務承攬、契約書、具結書、簽到表、獎助生 |
| `procedures/foreign-scholars.md` | 邀請國外學者、來訪接待、餐敘、機票款與生活費 |
| `procedures/official-documents.md` | 公文、簽呈、書函、計畫展延、代擬院稿 |
| `procedures/nstc-budget-change.md` | 國科會經費變更、流用、增列、調減 |
| `procedures/special-fees.md` | 訂閱軟體、投稿費、潤稿費、註冊費、審查費、主持人費、勞健保、資料購買 |
| `procedures/funding.md` | 經費類型、哪個會計管哪類經費、科目怎麼勾 |
| `procedures/contacts.md` | 承辦人、分機、系統網址、常用地址 |
| `lab-profile/` | **本研究室的私人資料**，一年度一檔、未進版控（挑檔規則見下節） |
| `templates/` | 表單與範本；官方空白表單一律連到所內專區現場下載 |
| `setup/` | 各家 AI 工具的安裝輔助檔（不是流程內容） |

流程內容全部在 `procedures/`；根目錄只有兩個入口檔（`SKILL.md`、`AGENTS.md`）和
`lab-profile/`、`procedures/`、`templates/`、`setup/` 四個資料夾。

## 挑選研究室設定檔

**沒有 `lab-profile.md` 這個檔案。** 文件裡寫的 **`lab-profile/` 第 N 節**，指的是這個資料夾裡
**最新那一檔**的第 N 節：先列出資料夾，跳過範本，取年度最新、同年度取序號最大的那一個。
舊檔只在需要比對歷年數字時才讀。

節次固定為三節：一 研究室基本資料／二 經費清單（2.1 所上／2.2 院內主題／2.3 院外／2.4 已結案）／
三 帳號、雲端與研究室慣例（3.1 訂閱帳號信箱／3.2 軟體訂閱清單／3.3 記帳雲端／3.4 特有慣例）。

## 工作守則

1. **不要杜撰。** 老師姓名、經費金額、計畫代碼／院內編號、訂閱帳號信箱，
   一律以 `lab-profile/` 中**目前有效的那一檔**為準（挑法見上節）。凡是任務牽涉
   「這間研究室的老師是誰」「用哪筆經費」「計畫代碼是什麼」，**先讀那一檔**。
   若資料夾裡只有範本，請使用者先從 `lab-profile/lab-profile-example.md` 複製一份填好，不要自己猜。
   （承辦人姓名與分機不屬私人資料，見 `procedures/contacts.md`。）
2. **金額、期限、門檻照抄文件**，不要自行估算或四捨五入成「大約」。
   匯率換算要寫出計算過程（見 `procedures/reimbursement.md`）。
3. **個資只用佔位符。** 產出任何會進版控的檔案時，身分證字號、戶籍地址、電話一律寫成
   `〔身分證字號〕` 這類佔位符。真實個資只寫在紙本或 `lab-profile/`、研究室自己的雲端。
4. **規則年年改。** 回答涉及表單格式或金額門檻時，提醒使用者到經濟所所內專區確認最新版：
   <https://www.econ.sinica.edu.tw/ioecin/pages/126>。文件中標 `TODO` 的地方代表判讀信心較低，
   要主動說明「這點請跟承辦人再確認」。
5. **發現文件與現行規定不符，就地修正這份文件**並告訴使用者要 commit／push，
   讓知識庫保持在最新狀態。通用流程改子檔案，只有自家研究室的資料改 `lab-profile/`。
6. 使用者用中文提問就用中文回答。
